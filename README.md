This project is a demo to use Spark streaming, Kafka and Hive for logging.

To run the jar file:
spark-submit --master yarn --class com.demo.SparkKafkaStreaming /yourpath/spark_kafka_hive_logging_2.11-0.1.jar
